@model CustomerApp.Models.CustomerViewModel

@{
    ViewBag.Title = "Customer Details";
}

<h2>Details</h2>

<div>
    <h4>Customer</h4>
    <hr />
   @{ Html.RenderPartial("_CustomerData", Model); }
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
